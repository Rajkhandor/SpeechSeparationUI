Open `main.ui` file in qt-designer to make updates to UI

Run the following command after making the updates
pyuic5 -x main.ui -o main.py

Videoplayer class extends the window created using `main.ui` class to add functionality to UI.

